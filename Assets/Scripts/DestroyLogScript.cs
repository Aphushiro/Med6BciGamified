using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyLogScript : MonoBehaviour
{
    // Start is called before the first frame update
   public void DestroyLog()
    {
        Destroy(gameObject);  
    }
}
